[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "rulix"
version = "0.1.0"
description = "Lightweight, embeddable rule-based interpreter for Python"
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths  = ["tests"]
